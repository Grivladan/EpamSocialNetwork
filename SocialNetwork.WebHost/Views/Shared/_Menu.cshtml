<div class="btn-group-vertical">
    <a href="@Url.Action("Edit", "Profile", new { id = @Model.Id })" class="btn btn-primary btn-large">
        My Profile
        <span class="glyphicon glyphicon-home" aria-hidden="true"></span>
    </a>
    <a href="@Url.Action("GetUserFriends", "Home", null)" class="btn btn-primary btn-large">
        Friends
        <span class="glyphicon glyphicon-user" aria-hidden="true"></span>
    </a>
    <a href="@Url.Action("GetUserMessages", "Message", new { id = @Model.Id })" class="btn btn-primary btn-large">
        Messages
        <span class="glyphicon glyphicon-envelope" aria-hidden="true"></span>
    </a>
    <a href="@Url.Action("GetPostsByUser", "Post", new { id = @Model.Id })" class="btn btn-primary btn-large">
        Posts
        <i class="glyphicon glyphicon-list-alt"></i>
    </a>
</div>
